
{{#each posts as |post|}}
    <div class="card" style="width: 18rem">
      <div class="card-header">{{post.username}}</div>
      <ul class="list-group list-group-flush">
        <li class="list-group-item">User comment1</li>
      </ul>
    </div>
    {{/each}}
   

    
